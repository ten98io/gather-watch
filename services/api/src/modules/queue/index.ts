/** Queue module — placeholder registration; implementation follows. */
import type { ModulePlugin } from '../types';

const queueModule: ModulePlugin = { name: 'queue' };
export default queueModule;
